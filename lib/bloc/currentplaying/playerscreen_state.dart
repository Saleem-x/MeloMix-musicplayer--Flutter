part of 'playerscreen_bloc.dart';

class PlayerscreenState {
  final bool isloading;

  PlayerscreenState(this.isloading);
}

class PlayerscreenInitial extends PlayerscreenState {
  PlayerscreenInitial(super.isloading);
}
